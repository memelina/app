import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.memorygame4',
  appName: 'memory-gameTGM_LUNG',
  webDir: 'dist'
};

export default config;
